{
  "name": "part4",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "standard && nodemon index.js && lint",
    "start": "node index.js && lint",
    "lint": "standard --fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20",
    "standard": "^17.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "mongoose": "^6.10.0"
  }
}
